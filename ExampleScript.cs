using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ExampleScript : MonoBehaviour, iTriggerEnter, iTriggerExit
{
	//Code that receives the message from the child interface
	private void TriggerEnter(Collider col)
	{
		
	}

	//Code that receives the message from the child interface
	private void TriggerExit(Collider col)
	{
		
	}
}
